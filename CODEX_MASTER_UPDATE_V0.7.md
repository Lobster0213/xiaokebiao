# 小課表 App｜Codex 綜合更新任務 v0.7

請直接修改目前已放上 GitHub 的既有專案。先完整閱讀 repository、README、資料模型、Android／Web 架構、建置設定、版本號、簽章與 GitHub Release 流程。

## 開工限制

- 不要重新初始化專案。
- 不要重做目前首頁與既有視覺。
- 不要更換既有資料 key 或清除使用者資料。
- 先建立備份分支或 git commit。
- 所有新欄位都要有向後相容 migration。
- 每完成一階段就測試並建立獨立 commit。
- 若目前只是純 Web，先補齊 Android 原生／包裝層；不要假裝純網頁能直接安裝 APK。
- 不要只提供建議，請直接修改、測試、建置。

---

## 1. Android 應用程式內更新

### 使用流程
1. App 啟動或「設定 → 檢查更新」時讀取 GitHub 最新正式 Release。
2. 比較目前版本與最新版本。
3. 有新版時顯示版本、更新內容、APK 大小及「稍後／下載並更新」。
4. 使用者同意後，在 App 內背景下載 APK並顯示進度。
5. 下載完成後直接喚起 Android 系統安裝畫面，不跳到瀏覽器。
6. 保留系統覆蓋安裝確認，不可靜默安裝或繞過未知來源權限。

### 技術與安全
- 集中設定 GitHub owner/repo。
- 只讀正式 Release，排除 draft、prerelease。
- 只接受 release APK，排除 AAB、debug、source zip。
- package name 與簽章必須和現有 App 相同，versionCode 必須更高。
- 可依目前架構使用 DownloadManager／FileProvider／PackageInstaller 或對應原生橋接。
- Android 8+ 缺權限時先說明，再開啟此 App 的「允許安裝未知應用程式」設定。
- 權限返回後可繼續安裝，不必重新下載。
- 處理無網路、GitHub API 失敗、找不到 APK、下載中斷、空間不足、APK 損毀、簽章錯誤、使用者取消等狀態。
- 設定頁增加：目前版本、檢查更新、自動檢查開關、僅 Wi-Fi 下載、上次檢查時間、重新下載／繼續安裝。
- App 更新不得要求刪除資料。

---

## 2. 新增堂數

在學生「新增堂數」提供：

- 1、2、4、8、12、16、20 堂
- 自訂堂數

自訂只能輸入 1～999 的整數。儲存前顯示本次新增與新增後剩餘堂數。

新增 `LessonCreditTransaction` 或等效異動紀錄，至少記錄：

- studentId
- 類型：購買、贈送、人工修正、扣堂、還原、退款
- amount（新增為正、扣堂為負）
- reason
- relatedLessonId
- createdAt

學生詳情可查看堂數異動。避免雙擊重複新增。

---

## 3. 修正跨月份重複排課

目前建立 8 堂時不能跨月，甚至把課都擠在同月。請改成以實際日期每 7 天遞增，不受月份或年份邊界限制。

例如每週四 20:00，自 2026/08/06 起共 8 堂，必須建立：

08/06、08/13、08/20、08/27、09/03、09/10、09/17、09/24。

要求：

- 跨月、跨年、不同月份天數皆正確。
- 固定星期與時間不變。
- 儲存前預覽全部日期與最後一堂。
- 週／月視圖都要顯示完整。
- 每批建立 recurrenceGroupId。
- 建立完成提供「查看課表」與「復原本次排課」。

### 衝堂
儲存前即時檢查老師整體行程。時間區間重疊才算；20:00 結束與 20:00 開始不算衝突；取消課程可忽略。

紅字逐筆顯示：

`2026/09/10（四）20:00–21:00 與「王小明｜數學｜20:30–21:30」衝突`

保留「遇到衝堂時跳過該週，並在後續相同星期、相同時間補足原定堂數」功能，預設開啟。最多向後找 104 週，避免無限迴圈。不得擅自改時間或星期。

---

## 4. 當日最後一堂提醒

首頁只檢查今天有課的學生。

若該生為購買堂數制，且今天這堂完成並扣堂後會剩 0 堂，於今日課程上方顯示精簡置頂提醒：

`陳小安｜今天 20:00｜今天是最後 1 堂`

按鈕：

- 查看學生
- 新增堂數

完成並扣堂後改為「陳小安的堂數已用完」。若取消、不扣堂、刪除或新增堂數，提醒需即時重算。

多位學生時只顯示摘要「今天有 2 位學生即將用完堂數」，可展開，不要放多張大型卡片。

---

## 5. 試教課程

新增課程類型：

- 正式課程
- 試教課程

試教預設不扣正式堂數，可填：

- 試教費用
- 來源
- 聯絡資訊
- 備註
- 結果：待確認、已轉正式、未續課、待追蹤

提供「轉為正式學生」：

- 保留姓名與聯絡資訊
- 選擇初始堂數、正式費用與固定時間
- 不把試教自動算入正式已上堂數，除非老師明確勾選
- 舊課程 migration 預設為正式課程

---

## 6. 真正刪除課程

保留「取消課程」，並新增紅色「刪除課程」。

- 取消：保留紀錄，狀態改為取消。
- 刪除：從一般課表與列表移除。

刪除前二次確認。若已完成且曾扣堂，詢問：

- 刪除並加回 1 堂
- 僅刪除紀錄，不調整堂數
- 取消

優先採軟刪除 `deletedAt`。刪除後顯示 8 秒「課程已刪除｜復原」。

刪除與復原必須同步處理 lesson、lessonRecord、堂數交易、剩餘堂數、最後一堂提醒與 recurrenceGroup，不可留下孤兒資料，也不可重複扣堂或加回。

---

## 7. 首次使用教學與示範資料

新增：

- `hasCompletedOnboarding`
- `onboardingVersion`
- 示範資料標記 `isDemoData: true`

首次啟動顯示 4 步：

1. 歡迎使用小課表：快速排課、記錄堂數、查看行程。
2. 三項核心功能：固定排課、自動扣堂、最後一堂提醒。
3. 示範資料說明：可先操作，正式使用前可只清除示範資料。
4. 清除確認：只刪除示範學生、示範課程、示範紀錄，不會刪除手機其他檔案或自行建立資料。

第 3 步提供：

- 先使用示範資料看看
- 清除示範資料並開始

清除後直接引導「新增第一位學生」。

設定頁增加：

- 使用教學
- 重新觀看首次介紹
- 清除示範資料（只有存在示範資料時顯示）

不要把文字寫成「使用前須刪除全部資料」。

---

## 8. 清除全部資料安全機制

若保留「清除全部資料」：

1. 先建議下載完整 JSON 備份。
2. 列出會刪除老師、學生、課程、堂數與設定。
3. 要求輸入「確認清除」。
4. 完全符合才啟用紅色確認按鈕。
5. 不得刪除手機其他檔案。

---

## 9. 資料版本與一致性

新增集中 `dataVersion`，並為以下欄位做 idempotent migration：

- lessonType／試教欄位
- LessonCreditTransaction
- isDemoData
- deletedAt
- recurrenceGroupId
- onboarding 設定
- update 設定

要求：

- migration 前建立本機備份快照。
- migration 失敗時不覆蓋原資料，並允許匯出原始備份。
- JSON 匯出包含所有新資料且可重新匯入。
- 舊版 JSON 仍可匯入並補預設值。
- 完成課程、修改紀錄、刪除、復原都走集中 domain functions／transaction，避免半套更新。
- 所有送出按鈕處理中 disabled，避免雙擊。

---

## 10. 驗收

至少完成下列測試：

### 更新
- 有新版、無新版、Release 無 APK、下載中斷、未知來源未允許、使用者取消安裝。
- 下載不開瀏覽器，完成後開啟 Android 系統安裝畫面。
- 更新後原資料仍存在。

### 堂數
- 可新增 4 堂與任意整數堂數。
- 0、負數、小數不可儲存。
- 產生異動紀錄，雙擊不重複。

### 跨月排課
- 8 月排 8 堂能正確進入 9 月。
- 12 月可跨到隔年 1 月。
- 衝堂紅字明確，跳過後仍補足原定堂數。
- 可整批復原。

### 最後一堂
- 今天完成後剩 0 堂才提醒。
- 取消、不扣堂、刪除、復原、新增堂數後會重算。

### 試教
- 不扣正式堂數。
- 可記錄結果與轉正式。

### 刪除
- 已扣堂課程刪除時可選擇是否加回。
- 8 秒內可復原，且不重複調整堂數。

### 首次教學
- 只清除 `isDemoData: true`。
- 不刪除自行建立資料。
- 教學完成後不再自動出現，設定中可重看。

### 回歸
- 首頁、今日課程、週／月視圖、星期一起始、老師資訊、學生、單次／重複排課、複製上一堂、狀態、JSON／CSV、既有資料全部正常。

---

## 11. 開發順序

1. 備份與 migration
2. 跨月份重複排課
3. 堂數異動與 4 堂／自訂堂數
4. 安全刪除與復原
5. 試教
6. 最後一堂提醒
7. 首次教學與清除示範資料
8. Android GitHub Release 更新
9. 回歸測試與 README／Release 說明

完成後回報：

- 偵測到的技術架構
- 修改檔案
- migration
- GitHub Release／APK 流程
- 跨月排課修正
- 堂數與刪除還原規則
- 首次教學
- lint、typecheck、test、build 與 APK 產出位置
- GitHub Release 的建議命名與上傳方式
- 已知限制

遇到必要但無法推斷的 owner/repo、applicationId、簽章或 APK 命名，再列出待補資訊，不要猜測。
