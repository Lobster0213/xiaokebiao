# 小課表｜Codex 綜合 UX 更新任務 v0.8.2

請直接在目前既有的 `Lobster0213/xiaokebiao` 專案上進行增量修改。

目前專案已進入 v0.8.1，已具備 Web／PWA、Android WebView 包裝、固定排課與跨月排程、recurrenceGroupId、自動完成課程、堂數帳本、多科目、月視圖堂數、試教、改期、軟刪除與復原、首次教學、JSON／CSV 備份與 Android App 更新。

本次目標不是增加大型新模組，而是改善固定課程管理、扣堂規則、課後修正、資料安全與課表可讀性。

禁止重新初始化專案、重做首頁整體視覺、更改 applicationId、清除使用者現有資料、更換既有 localStorage key，或破壞 GitHub Pages、Android build、APK 更新、JSON／CSV。

目前產品決策維持：
- 課程到達結束時間後自動完成。
- 符合扣堂條件時自動扣堂一次。
- 若實際有請假、缺席、取消或改期，由老師後續編輯紀錄修正。
- 不重新加入「開始上課／記一堂／待確認」。

---

## 0. 開工前

1. 完整閱讀 README.md、index.html、app-domain.js、browser smoke tests、Android 專案與 GitHub Actions。
2. 建立分支 `feature/v0.8.2-series-and-record-management`。
3. 建立修改前 commit／備份。
4. 列出預計修改檔案。
5. 將 dataVersion 提升至下一版本。
6. 所有 migration 必須向後相容、idempotent、migration 前建立快照，失敗不覆蓋原資料。
7. 每完成一階段執行測試並建立獨立 commit。

---

## 1. 固定課程系列管理

所有相同 `recurrenceGroupId` 的課程視為同一固定課程系列。

具有 recurrenceGroupId 的課程詳情中新增：

固定課程系列
- 共 12 堂
- 已完成 3 堂
- 尚未上課 9 堂

操作：
- 查看系列
- 編輯系列
- 增加堂數
- 暫停／跳過
- 刪除系列

UI 保持精簡，不要同時顯示過多紅色按鈕。

---

## 2. 固定課程整批刪除

當使用者刪除 recurrenceGroupId 課程時，顯示：
1. 僅刪除此堂
2. 刪除此堂及之後
3. 刪除整個系列

預設「僅刪除此堂」。

每個選項要顯示實際受影響數量，例如：
- 僅刪除 8 月 12 日這堂
- 刪除此堂及之後，共 9 堂
- 刪除整個固定系列，共 12 堂

只能依 recurrenceGroupId 判斷系列。

若範圍包含已完成課程，確認畫面顯示：
- 尚未開始幾堂
- 已完成幾堂
- 其中幾堂曾扣堂

預設建議「只刪除尚未開始的課程」。

若包含已完成課程，再詢問：
- 刪除並還原已扣堂數
- 刪除但保留堂數紀錄
- 取消

所有堂數變更必須走 LessonCreditTransaction。

整批操作建立 `batchOperationId`。
刪除成功後顯示：
`已刪除 9 堂固定課程　[復原]`

提供 8 秒 Undo，只能復原本次 batchOperationId 的內容。

---

## 3. 固定課程整批編輯

系列操作支援：
1. 僅此堂
2. 此堂及之後
3. 整個系列

可修改：
- 星期
- 日期
- 開始時間
- 課程時長
- 科目
- 授課方式
- 地點

編輯前顯示預覽，例如：
`將修改 8 堂課程`
`原本：每週三 19:00–20:30`
`新的：每週四 20:00–21:30`

若造成衝堂：
- 不可直接儲存
- 列出衝突日期與課程
- 提供返回修改、跳過衝突日期、依既有規則往後補足

已完成課程預設不跟著整批修改。

---

## 4. 固定課程暫停／跳過

新增「暫停／跳過固定課程」。

支援：
- 單次跳過某一堂
- 暫停一段日期

暫停期間的課程使用 `skipped` 或等效狀態，不可視為刪除，也不可自動完成或扣堂。

暫停後可選：
- 不補課（預設）
- 往後補足原定堂數

---

## 5. 固定課程增加堂數

系列詳情增加「增加堂數」。

快捷：
- +1
- +2
- +4
- +8
- 自訂

規則：
- 從系列最後一堂之後開始
- 沿用同一星期、時間、學生、科目、模式、地點、時長
- 遇衝堂套用目前既有跳過並往後補邏輯
- 最多搜尋 104 週
- 新課程仍使用同一 recurrenceGroupId
- 建立前預覽新日期

完成後顯示新增數量與新的最後一堂，並提供 8 秒復原。

---

## 6. 學生請假／缺席扣堂規則

在「更多 → 老師／課程設定」增加預設扣堂規則：

- 學生請假：不扣堂／扣 1 堂
- 學生缺席：不扣堂／扣 1 堂
- 老師請假：預設不扣堂
- 取消課程：預設不扣堂

學生詳情增加：
- 沿用老師設定
- 自訂這位學生

學生個別至少可設定：
- studentLeave 是否扣堂
- absent 是否扣堂

資料模型可使用：
`student.deductionPolicy = { inheritTeacherPolicy, studentLeave, absent }`

老師選擇「學生請假」時顯示：
`依目前規則，本次將扣除 1 堂。`
並可選「本次改為不扣堂」。

單次覆蓋只作用於這堂，不修改長期設定。

---

## 7. 最近自動完成課程

不要放到首頁增加干擾。

在課表或學生詳情增加「最近課程紀錄」，提供今天與最近 7 天。

例如：
`8/09 王小明｜數學`
`已完成・系統自動完成・扣 1 堂`
`[修改紀錄]`

修改紀錄可以改為：
- 已完成並扣堂
- 已完成但不扣堂
- 學生請假
- 老師請假
- 缺席
- 取消
- 改期

修改後堂數交易必須自動 reconcile。

---

## 8. 課程紀錄修改可追蹤

LessonRecord 可加入 history：
- previousStatus
- newStatus
- previousDeductLesson
- newDeductLesson
- changedAt
- changeSource（system／teacher）

一般 UI 不必顯示技術欄位，只在「紀錄歷程」中查看。

---

## 9. 備份狀態

在「更多 → 資料與備份」顯示：
- 上次完整備份時間
- 目前學生數
- 課程數
- 堂數異動筆數
- 下載完整 JSON 備份
- 匯入備份

若從未備份顯示「尚未建立完整備份」。

超過 30 天未備份，在更多頁顯示低干擾提示，不要使用系統通知或首頁提醒。

每次匯入 JSON 前：
1. 先建立現有資料 snapshot
2. 驗證匯入檔
3. 成功才取代
4. 失敗維持原資料

成功匯入後，至少在同一次 session 可復原到匯入前資料。

---

## 10. 週視圖可讀性

手機七天同時顯示必須保留。

週視圖課程塊只顯示：
- 時間
- 學生名

不要塞地址、完整科目、備註。

點擊後下方 day agenda 顯示完整資訊。

字體最低建議：
- 時間 >= 9px
- 姓名 >= 10px

若空間不足，優先減少資訊與 padding，不要繼續縮小文字。

預設時段可維持 08:00–23:00；若該週有更早或更晚課程，應自動延伸，至少支援 05:00–24:00。

增加「回到現在」，若目前時間在可見區域顯示 current-time line。

---

## 11. 已購堂數 vs 未來已排堂數

學生詳情分開顯示：

堂數方案
- 剩餘可用 8 堂
- 已使用 4 堂

課程安排
- 未來已排 12 堂
- 最後一堂 10 月 28 日

若未來已排 > 剩餘可用，顯示橘色小提示：
`目前已排 12 堂，但剩餘可用 8 堂。後面 4 堂可能需要續課。`

不要阻止排課。

試教與按次計費不要顯示套票不足提示。

---

## 12. 課表篩選

課表頁增加低干擾「篩選」按鈕。

支援：
- 學生
- 科目
- 狀態

預設不篩選。

有篩選時顯示：
`已篩選：王小明・數學 [清除]`

不要永久佔據大量課表空間。

---

## 13. 本版不要新增

不要加入：
- 聊天
- 家長登入
- 線上付款
- 雲端會員
- 複雜營收圖表
- AI 自動排課
- 社群
- 多人協作

保持產品定位：
`個人老師快速排課、堂數與課後紀錄工具`

---

## 14. 資料模型建議

dataVersion 提升至下一版。

可能新增：
- Student.deductionPolicy
- TeacherSettings.deductionPolicy
- Lesson.skipReason
- Lesson.skippedAt
- LessonRecord.history
- AppSettings.lastFullBackupAt
- BatchOperation { id, type, affectedLessonIds, createdAt, undoUntil }

JSON 匯出／匯入必須支援新欄位，舊資料自動補預設值。

---

## 15. 驗收測試

固定課程：
1. 建立 12 堂後可刪除整個系列。
2. 可只刪第 5～12 堂。
3. 可只修改單堂。
4. 可修改此堂及之後。
5. 可修改整個系列尚未開始的課程。
6. 同一學生不同 recurrenceGroupId 不互相影響。
7. 整批刪除 8 秒內可復原。
8. 已完成課程不會無警告整批刪除。
9. 可增加 4 堂並沿用系列設定。
10. 可暫停兩週且不自動完成／扣堂。

扣堂規則：
1. 老師可設定學生請假是否扣堂。
2. 學生可覆蓋老師預設規則。
3. 單堂可臨時改成不扣堂。
4. 修改後交易帳本一致。
5. 不會重複扣堂或還原。

最近紀錄：
1. 自動完成後出現在最近紀錄。
2. 可快速修改為學生請假。
3. 修改後正確加回堂數。
4. 可查看紀錄歷程。

備份：
1. 顯示上次備份時間。
2. 下載成功後更新時間。
3. 匯入前建立 snapshot。
4. 匯入錯誤不破壞現有資料。
5. 成功匯入後可在 session 內復原。

週視圖：
1. 390px 顯示七天。
2. 文字可讀性改善。
3. 早於 08:00 的課程可自動延伸。
4. 點課程／日期後下方顯示完整資料。

堂數：
1. 分開顯示剩餘堂數與未來已排堂數。
2. 已排超過剩餘時提示但不阻止。
3. 按次計費與試教不顯示錯誤提示。

回歸確認：
- 首頁三層資訊架構
- 自動完成
- 多科目
- 180／自訂時長
- 月視圖堂數
- 試教
- 改期
- JSON／CSV
- Android 更新
- 首次教學
- 通知去重
全部正常。

---

## 16. 開發順序

1. data migration 與備份
2. 固定課程系列刪除／Undo
3. 固定課程整批編輯
4. 暫停／增加堂數
5. 扣堂規則
6. 最近自動完成紀錄
7. 備份狀態
8. 堂數 vs 已排數
9. 週視圖可讀性
10. 課表篩選
11. 完整回歸測試
12. 更新 README

---

## 17. 完成後回報

請回報：
1. 修改檔案
2. dataVersion／migration
3. recurrenceGroupId 系列管理
4. batchOperationId／Undo
5. 扣堂規則
6. 課後紀錄修正
7. 備份安全
8. 週視圖改善
9. 測試結果
10. Android debug APK 位置
11. 已知限制

請直接修改、測試、建置並 commit，不要只提供規劃。
