# 小課表 v0.7 驗收紀錄

驗收日期：2026-07-31（Asia/Taipei）

## 架構與備份

- 偵測架構：單檔 HTML/CSS/JavaScript Web App + PWA。
- 原資料 key：`xiaokebiao_mvp_v1`，未更名。
- 原始版本備份分支：`backup/v0.6-before-v0.7`。
- v0.7 新增 Android API 26+ 原生 WebView 包裝層。

## 自動測試

`npm test`：

- v0.6 → v0.7 migration 與重複 migration。
- 不安全 ID 與孤兒堂數交易拒絕匯入。
- 2026/08/06 起每週四 8 堂，正確排到 2026/09/24。
- 12 月跨到隔年 1 月。
- 相接時間不衝突、重疊才衝突、取消課程忽略。
- 衝堂跳過後以相同星期與時間補足。
- 重複完成不會重複扣堂。
- 已扣堂課程刪除、加回與復原一致。
- 試教預設不扣正式堂數。
- 清除示範資料保留自行建立資料。
- CSV 公式開頭安全處理。

`npm run lint`：

- `index.html` inline JavaScript 語法。
- `app-domain.js` 語法與 `dataVersion`。
- manifest、Service Worker 與 GitHub Pages 打包清單。

`npm run test:android:static`：

- GitHub `releases/latest`。
- `DownloadManager` App 內下載。
- `FileProvider` 與 Android 系統安裝 Intent。
- Android 8+ 未知來源設定。
- package、versionCode 與簽章驗證。
- debug／unsigned／test／source APK 排除。
- applicationId 與 release signing 外部設定。

## 390 × 844 瀏覽器互動驗收

Chrome DevTools 行動裝置模擬結果：

- viewport：390 × 844。
- document scroll width：390，沒有整頁橫向溢位。
- 首次教學 1/4 自動開啟。
- 週視圖共 7 天，第一天為週一。
- 月視圖共 42 個日期格。
- 試教表單會隱藏正式學生與固定重複欄位。
- 「清除全部資料」按鈕預設停用，只有完整輸入「確認清除」才啟用。
- runtime exception：0。

驗收截圖位於本機未追蹤的 `.tools/`：

- `v07-mobile-final-390x844-week.png`
- `v07-mobile-final-390x844.png`

## Android 建置驗收

- JDK：Temurin 17 可攜式工具。
- Gradle：8.11.1。
- compileSdk／targetSdk：35。
- minSdk：26。
- `:app:assembleDebug`：成功。
- 驗證 APK：`android/app/build/outputs/apk/debug/app-debug.apk`。
- APK 大小：2,406,849 bytes。
- APK SHA-256：`8F8AE33E583B51B0FFC860CA6C4877034E2BFED3BA6C5733CAEEB424C1A9B1F9`。
- APK 內確認包含 `assets/index.html` 與 `assets/app-domain.js`。
- Debug applicationId 使用明確的驗證用 `.dev` 身分，不可上傳正式 Release。

## 正式發布準備狀態

已完成：

- 永久 applicationId 已由專案擁有者確認並固定為 `io.github.lobster0213.xiaokebiao`。
- 已建立 RSA 4096-bit release keystore、本機受限復原備份，以及 GitHub Actions repository variable／四項簽章 secrets。
- 憑證 SHA-256：`65:27:1C:61:9C:1C:3D:56:1D:D2:41:E2:70:38:A9:AE:54:EB:41:C7:F9:07:51:64:0D:97:C5:74:95:34:13:50`。
- 已成功產生並以 `apksigner` 驗證正式 APK：
  - applicationId：`io.github.lobster0213.xiaokebiao`
  - versionCode／versionName：`7001`／`0.7.0`
  - minSdk／targetSdk：`26`／`35`
  - 本機候選 APK SHA-256：`15D09C9E07BDE249D6836804659B5E4830D5A46839DA80CB3D4F67B73C349904`
- 已公開 `v0.7.0-rc.1` Prerelease，GitHub Actions 簽章建置成功：
  - versionCode／versionName：`7000`／`0.7.0-rc.1`
  - APK SHA-256：`8CD783694AC4E1B8657A9C3E93E3C3F272DD0165FB6DFF99A85C05616D7132C5`
  - 從 GitHub 下載後重新驗證 applicationId、版本、SHA-256 與憑證指紋皆正確。
- 專案擁有者已於實體手機完成公開 RC 測試，並於 2026-07-31 回報驗收完畢。
- 正式 `v0.7.0` 將使用較高的 versionCode `7001`，確保可覆蓋安裝 RC 版。

發布後仍需由擁有者完成的金鑰保管事項：

1. 將整份本機簽章復原資料夾另存到離線且受保護的位置。

正式簽章資訊齊全後，推送 `v0.7.0` tag 即可由 `.github/workflows/android-release.yml` 建置並發布 `xiaokebiao-v0.7.0-release.apk`。
