請先完整檢查目前已放上 GitHub 的「小課表」repository，並閱讀 `CODEX_MASTER_UPDATE_V0.7.md`。

這是既有產品的綜合更新任務。禁止重新初始化、重做首頁或清除現有資料。請先建立備份分支／commit，確認技術架構與資料保存方式，再依文件指定順序分階段實作；每階段完成後測試並建立獨立 commit。

Android 更新不可靜默安裝，也不可跳到瀏覽器；下載完成後必須喚起 Android 系統安裝確認畫面。

請直接修改、測試與建置，不要只提供建議或程式碼片段。完成後依文件要求完整回報。
